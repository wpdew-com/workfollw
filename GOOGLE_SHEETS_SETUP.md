# Интеграция с Google Sheets для авторизации

## 📋 Что было создано

1. **auth-google-sheets.js** - обновленная система авторизации с поддержкой Google Sheets
2. **google-apps-script.gs** - серверный код для Google Apps Script

## 🚀 Инструкция по настройке

### Вариант 1: Google Apps Script (Рекомендуется - более безопасно)

#### Шаг 1: Создайте Google Таблицу

1. Откройте [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу
3. Создайте заголовки в первой строке:
   ```
   | username | password | email          | registered_date |
   | admin    | admin100 | admin@test.com | 2025-01-01      |
   | user     | 12345    | user@test.com  | 2025-01-01      |
   ```

#### Шаг 2: Настройте Apps Script

1. В Google Таблице: **Расширения** → **Apps Script**
2. Удалите весь код по умолчанию
3. Скопируйте содержимое файла **google-apps-script.gs**
4. Нажмите **Сохранить** (💾)

#### Шаг 3: Разверните веб-приложение

1. Нажмите **Развернуть** → **Новое развертывание**
2. Выберите тип: **Веб-приложение**
3. Настройки:
   - **Описание**: "Auth API"
   - **Выполнять как**: "Я"
   - **У кого есть доступ**: "Все"
4. Нажмите **Развернуть**
5. **Скопируйте URL веб-приложения** (выглядит как: `https://script.google.com/macros/s/...../exec`)

#### Шаг 4: Обновите код сайта

1. Откройте `auth-google-sheets.js`
2. Найдите строку:
   ```javascript
   const GOOGLE_APPS_SCRIPT_URL = 'YOUR_GOOGLE_APPS_SCRIPT_URL_HERE';
   ```
3. Вставьте скопированный URL:
   ```javascript
   const GOOGLE_APPS_SCRIPT_URL = 'https://script.google.com/macros/s/.../exec';
   ```
4. Убедитесь что режим установлен:
   ```javascript
   const AUTH_MODE = 'apps-script';
   ```

#### Шаг 5: Замените auth.js

В файлах `login.html`, `courses.html` и `index.html` замените:
```html
<script src="auth.js"></script>
```
на:
```html
<script src="auth-google-sheets.js"></script>
```

---

### Вариант 2: Публичная CSV таблица (Проще, но менее безопасно)

#### Шаг 1: Создайте таблицу

1. Создайте Google Таблицу со структурой:
   ```
   username,password
   admin,admin100
   user,12345
   ```

#### Шаг 2: Опубликуйте как CSV

1. **Файл** → **Опубликовать в Интернете**
2. Выберите **Лист 1**
3. Формат: **Значения, разделенные запятыми (.csv)**
4. Нажмите **Опубликовать**
5. **Скопируйте ссылку**

#### Шаг 3: Обновите код

В `auth-google-sheets.js`:
```javascript
const GOOGLE_SHEET_CSV_URL = 'ВАШ_CSV_URL';
const AUTH_MODE = 'csv';
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте Вариант 1** (Apps Script) - данные не публичны
2. **Хешируйте пароли** в таблице:
   - Используйте онлайн-генератор SHA-256
   - Храните хеши вместо паролей
3. **Ограничьте доступ** к таблице
4. **Используйте HTTPS** для сайта

### Для хеширования паролей:

1. Перейдите на https://emn178.github.io/online-tools/sha256.html
2. Введите пароль (например, `admin100`)
3. Скопируйте SHA-256 хеш
4. Вставьте в таблицу вместо обычного пароля

---

## 🧪 Тестирование

После настройки протестируйте:

1. Откройте `login.html`
2. Введите учетные данные из таблицы
3. Проверьте редирект на `courses.html`
4. Убедитесь что защита работает

---

## 📝 Добавление новых пользователей

Просто добавьте новую строку в Google Таблицу:
```
username | password  | email          | registered_date
newuser  | pass123   | new@test.com   | 2025-11-19
```

Изменения применяются мгновенно!

---

## ⚠️ Важные замечания

1. **Google Apps Script имеет лимиты**:
   - 20,000 запросов в день (бесплатно)
   - Задержка ~1-2 секунды на запрос

2. **Для production**:
   - Используйте настоящую базу данных
   - Добавьте капчу
   - Включите двухфакторную аутентификацию

3. **CORS**: Apps Script автоматически поддерживает CORS

---

## 🎯 Дополнительные функции

В `google-apps-script.gs` уже есть:
- ✅ Аутентификация
- ✅ Регистрация новых пользователей
- ✅ Проверка существующих пользователей

Для активации регистрации - добавьте форму регистрации на сайт!
